#!/bin/bash

# Install the required Radix UI toast dependency
npm install @radix-ui/react-toast

echo "✅ Toast dependency installed!"
echo "The toast components are already included in the project files."
echo "No need to run shadcn add toast - everything is ready!"
